// Declaring Variable with Let
let age = 25
console.log(age)

age = 67
console.log(age)

// Declaring Variable with Var (Not Used anymore/deprecated)
var name = "Todd"
console.log(name)

name = "Ashley"
console.log(name)

// Declaring Variable with Const (Best for Functions)
const hasGraduated = true
console.log(hasGraduated)

// Reassigning causes an error
// hasGraduated = false
// console.log(hasGraduated)