// Functions 
// parameters - when creating a function
function greeting(name, message) {
    console.log(`${message}, ${name}!!`)
}

// arguments - when calling a function 
greeting("Eyimofe", "Wash your clothes")


// Arrow functions - Use this
const sum = (a, b) => {
    return a + b
}

console.log(sum(10, 25))